import { darken } from "@/helpers";

export const Footer = () => {
    return (
        <footer className="flex w-full flex-row items-center justify-center py-3 bg-darken">
            <span className="text-sm font-bold text-white">Made with ❤️ by Daniel Sáez</span>
        </footer>
    );
};
